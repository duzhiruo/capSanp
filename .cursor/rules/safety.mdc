---
description: 安全红线规则，定义禁止操作和需要授权的操作
alwaysApply: true
---

# 安全红线

## 禁止操作（任何情况下）

- 禁止 `git push --force` 到任何分支
- 禁止在代码中硬编码 API Key、密码、token
- 禁止删除或覆盖 `.env` 中的密钥
- 禁止跳过 pre-commit hook（`--no-verify`）

## 需要 LD 授权的操作

- 数据库 schema 变更（新建 migration 文件前需确认）
- 修改已有 API 的入参/出参结构
- 引入新的第三方依赖
- 删除已有代码文件
- 修改 Agent 工作流步骤

## 提交前检查

- [ ] 无 API Key / 密码 泄露
- [ ] 无 SQL 注入风险
- [ ] 错误响应不暴露内部细节
- [ ] 日志不打印敏感数据
